# P1 Features Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build P1 features: UserProfile foundation, Onboarding (welcome + checklist), Dashboard (stats + activity + cards), Template Gallery, and Settings preferences wiring.

**Architecture:** UserProfile is a one-to-one extension of Django's User model, storing onboarding state and user preferences. Onboarding uses a redirect-based flow: `LOGIN_REDIRECT_URL=/dashboard/` → dashboard checks `onboarding_completed` → redirects to welcome if false. Dashboard shows stats, activity feed, and resume cards. Template gallery renders previews of all 3 existing templates. Settings is already partially built — only needs preference forms wired to UserProfile.

**Tech Stack:** Django 4.2, `crispy-forms`, `crispy-tailwind`, Tailwind CSS (via CDN), reportlab (already installed).

---

## Shared Foundation

### Task 1: UserProfile Model + Signals

**Files:**
- Modify: `d:/EliteSttack/accounts/models.py`
- Create: `d:/EliteSttack/accounts/signals.py`
- Modify: `d:/EliteSttack/accounts/apps.py`
- Create: `d:/EliteSttack/accounts/migrations/0001_initial_profile.py`
- Modify: `d:/EliteSttack/accounts/tests.py`
- Modify: `d:/EliteSttack/resumes/views.py:dashboard`
- Modify: `d:/EliteSttack/accounts/views.py:signup_view`
- Modify: `d:/EliteSttack/accounts/settings_views.py:settings`
- Modify: `d:/EliteSttack/placement_copilot/settings.py`

---

- [ ] **Step 1: Add UserProfile model to accounts/models.py**

Replace the empty `models.py` content with:

```python
from django.db import models
from django.contrib.auth.models import User
from django.db.models.signals import post_save
from django.dispatch import receiver


class UserProfile(models.Model):
    """Extension of Django User for onboarding and preferences."""
    TEMPLATE_CHOICES = [
        ('classic', 'Classic'),
        ('modern', 'Modern'),
        ('minimal', 'Minimal'),
    ]
    PAPER_SIZE_CHOICES = [
        ('letter', 'US Letter'),
        ('a4', 'A4'),
    ]

    user = models.OneToOneField(
        User,
        on_delete=models.CASCADE,
        related_name='profile'
    )
    onboarding_completed = models.BooleanField(default=False)
    default_template = models.CharField(
        max_length=20,
        choices=TEMPLATE_CHOICES,
        default='classic'
    )
    default_paper_size = models.CharField(
        max_length=10,
        choices=PAPER_SIZE_CHOICES,
        default='letter'
    )
    email_opt_out = models.BooleanField(default=False)

    def __str__(self):
        return f"Profile for {self.user.username}"


@receiver(post_save, sender=User)
def create_user_profile(sender, instance, created, **kwargs):
    """Create a UserProfile whenever a new User is created."""
    if created:
        UserProfile.objects.create(user=instance)


@receiver(post_save, sender=User)
def save_user_profile(sender, instance, **kwargs):
    """Save UserProfile whenever User is saved."""
    instance.profile.save()
```

---

- [ ] **Step 2: Register signals.py in accounts/apps.py**

Read `d:/EliteSttack/accounts/apps.py` first. It likely has a standard Apps class. Update it to:

```python
from django.apps import AppConfig


class AccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'accounts'

    def ready(self):
        import accounts.signals  # noqa
```

---

- [ ] **Step 3: Run makemigrations**

Run: `cd d:/EliteSttack && python manage.py makemigrations accounts`
Expected: Migration file created at `accounts/migrations/0002_auto_..._userprofile.py` (or 0001 if no migrations exist yet).

---

- [ ] **Step 4: Apply migrations**

Run: `python manage.py migrate`
Expected: `Applying accounts.000X... OK`

---

- [ ] **Step 5: Update dashboard view to use onboarding_completed**

Read `d:/EliteSttack/resumes/views.py` lines 29-40. Replace the `dashboard` function with:

```python
@login_required
def dashboard(request):
    """Dashboard view — show stats, activity, and resume cards."""
    user = request.user

    # Sync computed status on all resumes
    resumes = Resume.objects.filter(user=user).select_related('user')
    for r in resumes:
        r.status = r.computed_status
        r.save(update_fields=['status'])

    # Stats
    total = resumes.count()
    complete = resumes.filter(status='complete').count()
    draft = resumes.filter(status='draft').count()
    last_edited = resumes.order_by('-updated_at').first()

    # Activity feed — last 5 updated resumes
    activity = resumes.order_by('-updated_at')[:5]

    return render(request, 'resumes/dashboard.html', {
        'resumes': resumes,
        'stats': {
            'total': total,
            'complete': complete,
            'draft': draft,
            'last_edited': last_edited.updated_at if last_edited else None,
        },
        'activity': activity,
    })
```

---

- [ ] **Step 6: Update signup_view to redirect to welcome page**

Read `d:/EliteSttack/accounts/views.py` lines 14-18. Change the redirect target from `'dashboard'` to `'welcome'`:

```python
            return redirect('welcome')
```

---

- [ ] **Step 7: Add template filter for relative time in base template**

Read `d:/EliteSttack/templates/base.html`. Add a custom template tag for relative timestamps.

Create file: `d:/EliteSttack/resumes/templatetags/resume_extras.py`

```python
from django import template
from django.utils import timezone
from datetime import timedelta

register = template.Library()


@register.filter
def relative_time(dt):
    """Return a human-readable relative time string."""
    if not dt:
        return ''
    now = timezone.now()
    diff = now - dt
    total_seconds = diff.total_seconds()
    if total_seconds < 60:
        return 'just now'
    elif total_seconds < 3600:
        minutes = int(total_seconds / 60)
        return f'{minutes} minute{"s" if minutes != 1 else ""} ago'
    elif total_seconds < 86400:
        hours = int(total_seconds / 3600)
        return f'{hours} hour{"s" if hours != 1 else ""} ago'
    elif total_seconds < 604800:
        days = int(total_seconds / 86400)
        return f'{days} day{"s" if days != 1 else ""} ago'
    else:
        return dt.strftime('%b %d, %Y')
```

Create file: `d:/EliteSttack/resumes/templatetags/__init__.py`

```python
```

---

- [ ] **Step 8: Register the template tag library**

Run: `python manage.py check`
Expected: 0 issues

---

- [ ] **Step 9: Write tests for UserProfile model**

Read `d:/EliteSttack/accounts/tests.py`. Add to the existing file:

```python
from django.test import TestCase
from django.contrib.auth.models import User
from accounts.models import UserProfile


class UserProfileModelTest(TestCase):
    """Tests for the UserProfile model."""

    def test_user_profile_created_on_user_creation(self):
        """A UserProfile is automatically created when a User is saved."""
        user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )
        self.assertTrue(hasattr(user, 'profile'))
        self.assertEqual(user.profile.onboarding_completed, False)
        self.assertEqual(user.profile.default_template, 'classic')
        self.assertEqual(user.profile.default_paper_size, 'letter')
        self.assertEqual(user.profile.email_opt_out, False)

    def test_user_profile_str(self):
        """UserProfile __str__ returns the username."""
        user = User.objects.create_user(
            username='testuser2',
            email='test2@example.com',
            password='testpass123'
        )
        self.assertEqual(str(user.profile), 'Profile for testuser2')

    def test_onboarding_completed_defaults_to_false(self):
        """New profiles have onboarding_completed=False."""
        user = User.objects.create_user(
            username='testuser3',
            email='test3@example.com',
            password='testpass123'
        )
        self.assertFalse(user.profile.onboarding_completed)

    def test_template_choices(self):
        """Template choices are valid."""
        user = User.objects.create_user(
            username='testuser4',
            email='test4@example.com',
            password='testpass123'
        )
        user.profile.default_template = 'modern'
        user.profile.save()
        self.assertEqual(user.profile.default_template, 'modern')

    def test_paper_size_choices(self):
        """Paper size choices are valid."""
        user = User.objects.create_user(
            username='testuser5',
            email='test5@example.com',
            password='testpass123'
        )
        user.profile.default_paper_size = 'a4'
        user.profile.save()
        self.assertEqual(user.profile.default_paper_size, 'a4')
```

---

- [ ] **Step 10: Run tests for UserProfile**

Run: `python manage.py test accounts.tests.UserProfileModelTest --verbosity=1`
Expected: 5 PASS

---

- [ ] **Step 11: Write tests for dashboard onboarding redirect**

Add to `d:/EliteSttack/resumes/tests.py` (or create if not found at `d:/EliteSttack/resumes/tests.py`):

```python
from django.test import TestCase, Client
from django.contrib.auth.models import User
from django.urls import reverse
import json


class OnboardingRedirectTest(TestCase):
    """Tests for onboarding redirect behavior."""

    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )

    def test_new_user_redirected_to_welcome(self):
        """New authenticated user with no resumes is redirected to welcome."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('dashboard'))
        self.assertRedirects(response, reverse('welcome'))

    def test_returning_user_redirected_to_resume_list(self):
        """Authenticated user with resumes is redirected to resume list."""
        self.client.login(username='testuser', password='testpass123')
        # Manually create a UserProfile for this user
        self.user.profile.onboarding_completed = True
        self.user.profile.save()
        # Create a resume
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('dashboard'))
        self.assertRedirects(response, reverse('resume_list'))
```

Note: The dashboard currently redirects to `resume_list` after checking for resumes. The onboarding logic will live in `welcome.html` skip flow and the dashboard itself. The test above verifies behavior after Task 2 is complete.

---

- [ ] **Step 12: Commit**

```bash
git add accounts/models.py accounts/signals.py accounts/apps.py accounts/tests.py resumes/views.py accounts/views.py resumes/templatetags/resume_extras.py resumes/templatetags/__init__.py
git commit -m "feat(P1): add UserProfile model with signals for onboarding and preferences

- UserProfile: onboarding_completed, default_template, default_paper_size, email_opt_out
- Auto-create profile on User creation via post_save signal
- Dashboard shows stats, activity, resume cards (new template)
- Add relative_time template filter for activity feed
- Update signup to redirect new users to welcome page"
```

---

## P1.1 Onboarding

### Task 2: Welcome Page Skip + Onboarding Checklist Widget

**Files:**
- Modify: `d:/EliteSttack/accounts/urls.py`
- Create: `d:/EliteSttack/accounts/onboarding_views.py`
- Modify: `d:/EliteSttack/templates/onboarding/welcome.html`
- Modify: `d:/EliteSttack/templates/resumes/resume_builder.html`
- Create: `d:/EliteSttack/templates/resumes/onboarding_checklist.html`

---

- [ ] **Step 1: Create onboarding views**

Create file: `d:/EliteSttack/accounts/onboarding_views.py`

```python
from django.shortcuts import redirect
from django.contrib.auth.decorators import login_required


@login_required
def onboarding_skip(request):
    """Mark onboarding as complete and redirect to dashboard."""
    request.user.profile.onboarding_completed = True
    request.user.profile.save(update_fields=['onboarding_completed'])
    return redirect('dashboard')


@login_required
def onboarding_complete(request):
    """Mark onboarding as complete (called after first resume is created)."""
    request.user.profile.onboarding_completed = True
    request.user.profile.save(update_fields=['onboarding_completed'])
    return redirect('dashboard')
```

---

- [ ] **Step 2: Add onboarding URLs**

Read `d:/EliteSttack/accounts/urls.py`. Add to the urlpatterns list:

```python
    # Onboarding
    path('onboarding/skip/', onboarding_views.onboarding_skip, name='onboarding_skip'),
    path('onboarding/complete/', onboarding_views.onboarding_complete, name='onboarding_complete'),
```

Also add the import at the top:
```python
from . import onboarding_views
```

---

- [ ] **Step 3: Update welcome page template**

Read `d:/EliteSttack/templates/onboarding/welcome.html`. Replace lines 1-71 with this updated version that personalizes with the user's first name and adds the skip link:

```html
{% extends 'base.html' %}
{% block title %}Welcome - Placement Copilot{% endblock %}

{% block content %}
{% with first_name=request.user.first_name|default:"there" %}
<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8 bg-gradient-to-br from-blue-50 to-indigo-100">
    <div class="max-w-lg w-full space-y-8 text-center">
        <!-- Logo/Icon -->
        <div class="flex justify-center">
            <div class="w-24 h-24 bg-blue-600 rounded-2xl flex items-center justify-center shadow-lg">
                <svg class="w-12 h-12 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
            </div>
        </div>

        <!-- Welcome Text -->
        <div>
            <h1 class="text-4xl font-bold text-gray-900 mb-4">
                Welcome, {{ first_name }}!
            </h1>
            <p class="text-xl text-gray-600 mb-2">
                Create professional resumes that help you stand out
            </p>
            <p class="text-gray-500">
                Build, manage, and export your resume in minutes
            </p>
        </div>

        <!-- Features -->
        <div class="grid grid-cols-3 gap-4 py-6">
            <div class="text-center">
                <div class="w-12 h-12 mx-auto bg-blue-100 rounded-full flex items-center justify-center mb-2">
                    <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                </div>
                <p class="text-sm font-medium text-gray-700">Easy Builder</p>
            </div>
            <div class="text-center">
                <div class="w-12 h-12 mx-auto bg-green-100 rounded-full flex items-center justify-center mb-2">
                    <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                </div>
                <p class="text-sm font-medium text-gray-700">Save & Edit</p>
            </div>
            <div class="text-center">
                <div class="w-12 h-12 mx-auto bg-purple-100 rounded-full flex items-center justify-center mb-2">
                    <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
                    </svg>
                </div>
                <p class="text-sm font-medium text-gray-700">Export PDF</p>
            </div>
        </div>

        <!-- CTA Buttons -->
        <div class="space-y-4">
            <a href="{% url 'resume_create' %}" class="w-full flex items-center justify-center px-8 py-4 border border-transparent text-lg font-medium rounded-xl text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 shadow-lg">
                Create Your First Resume
                <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
            </a>
            <a href="{% url 'onboarding_skip' %}" class="text-gray-500 hover:text-gray-700 text-sm">
                Skip for now
            </a>
        </div>
    </div>
</div>
{% endwith %}
{% endblock %}
```

Note: The template uses `{% load account_extras %}` — if that templatetag doesn't exist, remove the `{% load account_extras %}` line and `{% with first_name=... %}` block and just use "Welcome!" as the heading.

---

- [ ] **Step 4: Create onboarding checklist widget template**

Create file: `d:/EliteSttack/templates/resumes/onboarding_checklist.html`

```html
{% load resume_extras %}

<div id="onboarding-checklist" class="bg-white rounded-lg shadow-md p-4 mb-4">
    <div class="flex justify-between items-center mb-3">
        <h3 class="text-sm font-semibold text-gray-700">Getting Started</h3>
        <button id="checklist-collapse" class="text-gray-400 hover:text-gray-600">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
            </svg>
        </button>
    </div>
    <ul class="space-y-2 text-sm" id="checklist-items">
        <li data-section="personal" class="flex items-center gap-2 text-gray-500">
            <span class="checklist-icon">○</span>
            Add your personal info
        </li>
        <li data-section="experience" class="flex items-center gap-2 text-gray-500">
            <span class="checklist-icon">○</span>
            Add work experience
        </li>
        <li data-section="education" class="flex items-center gap-2 text-gray-500">
            <span class="checklist-icon">○</span>
            Add education
        </li>
        <li data-section="skills" class="flex items-center gap-2 text-gray-500">
            <span class="checklist-icon">○</span>
            Add skills
        </li>
        <li data-section="preview" class="flex items-center gap-2 text-gray-500">
            <span class="checklist-icon">○</span>
            Preview & export
        </li>
    </ul>
    <div id="checklist-complete" class="hidden mt-3 p-2 bg-green-50 rounded text-green-700 text-sm font-medium text-center">
        You're all set! Great resume ready.
    </div>
</div>

<script>
(function() {
    var checklist = document.getElementById('onboarding-checklist');
    if (!checklist) return;

    var collapseBtn = document.getElementById('checklist-collapse');
    var items = document.querySelectorAll('#checklist-items li');

    // Section map: form field selectors → section name
    var sectionMap = {
        '#id_full_name, #id_email, #id_phone': 'personal',
        '.experience-section': 'experience',
        '.education-section': 'education',
        '.skills-section': 'skills',
        '#preview-panel': 'preview'
    };

    function updateChecklist() {
        var allDone = true;
        items.forEach(function(item) {
            var section = item.dataset.section;
            var done = false;
            if (section === 'personal') {
                var name = document.querySelector('#id_full_name');
                var email = document.querySelector('#id_email');
                done = name && name.value.trim() && email && email.value.trim();
            } else if (section === 'experience') {
                var expSection = document.querySelector('.experience-section');
                done = expSection && expSection.querySelectorAll('input[name="experience_company"]').length > 0;
            } else if (section === 'education') {
                var eduSection = document.querySelector('.education-section');
                done = eduSection && eduSection.querySelectorAll('input[name="education_school"]').length > 0;
            } else if (section === 'skills') {
                var skillSection = document.querySelector('.skills-section');
                done = skillSection && skillSection.querySelectorAll('input[name="skill_name"]').length > 0;
            } else if (section === 'preview') {
                done = false; // User manually marks this by exporting
            }
            var icon = item.querySelector('.checklist-icon');
            if (done) {
                icon.textContent = '✓';
                item.classList.remove('text-gray-500');
                item.classList.add('text-green-600', 'font-medium');
            } else {
                icon.textContent = '○';
                item.classList.add('text-gray-500');
                item.classList.remove('text-green-600', 'font-medium');
                allDone = false;
            }
        });
        var completeMsg = document.getElementById('checklist-complete');
        if (completeMsg) {
            completeMsg.classList.toggle('hidden', !allDone);
        }
    }

    // Attach input listeners
    ['#id_full_name', '#id_email', '#id_phone'].forEach(function(sel) {
        var el = document.querySelector(sel);
        if (el) el.addEventListener('input', updateChecklist);
    });

    // Observe dynamic sections for added entries
    ['experience', 'education', 'skills'].forEach(function(section) {
        var sectionEl = document.querySelector('.' + section + '-section');
        if (sectionEl) {
            var observer = new MutationObserver(updateChecklist);
            observer.observe(sectionEl, { childList: true, subtree: true });
        }
    });

    // Collapse toggle
    if (collapseBtn) {
        collapseBtn.addEventListener('click', function() {
            var list = document.getElementById('checklist-items');
            var complete = document.getElementById('checklist-complete');
            var isHidden = list.classList.toggle('hidden');
            if (complete) complete.classList.toggle('hidden', isHidden || complete.classList.contains('hidden'));
        });
    }

    updateChecklist();
})();
</script>
```

---

- [ ] **Step 5: Add checklist to builder template**

Read `d:/EliteSttack/templates/resumes/resume_builder.html`. Find the left panel (form side) and add the checklist widget at the top of it.

The builder likely has a layout with form on the left and preview on the right. After the opening `<div class="...">` of the left panel (before the `<form>` tag), add:

```html
{% if not user.profile.onboarding_completed or user.resumes.all|length == 0 %}
    {% include 'resumes/onboarding_checklist.html' %}
{% endif %}
```

---

- [ ] **Step 6: Update resume_create to mark onboarding complete**

Read `d:/EliteSttack/resumes/views.py` lines 44-71. After `resume.save()` in `resume_create`, add:

```python
            # Mark onboarding as complete on first resume creation
            if not request.user.profile.onboarding_completed:
                request.user.profile.onboarding_completed = True
                request.user.profile.save(update_fields=['onboarding_completed'])
```

---

- [ ] **Step 7: Write onboarding tests**

Add to `d:/EliteSttack/accounts/tests.py`:

```python
from django.test import TestCase, Client
from django.contrib.auth.models import User
from django.urls import reverse


class OnboardingTest(TestCase):
    """Tests for onboarding flow."""

    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )

    def test_onboarding_skip_marks_complete(self):
        """POST to onboarding_skip sets onboarding_completed=True."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.post(reverse('onboarding_skip'))
        self.user.profile.refresh_from_db()
        self.assertTrue(self.user.profile.onboarding_completed)
        self.assertRedirects(response, reverse('dashboard'))

    def test_onboarding_skip_requires_login(self):
        """Onboarding skip redirects unauthenticated users to login."""
        response = self.client.post(reverse('onboarding_skip'))
        self.assertEqual(response.status_code, 302)
        self.assertIn('login', response.url)

    def test_welcome_page_shows_personalized_greeting(self):
        """Welcome page displays user's first name."""
        self.client.login(username='testuser', password='testpass123')
        self.user.first_name = 'Alice'
        self.user.save()
        response = self.client.get(reverse('welcome'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Welcome, Alice!')

    def test_welcome_page_fallback_for_empty_first_name(self):
        """Welcome page shows 'Welcome!' when first_name is empty."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('welcome'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Welcome, there!')

    def test_checklist_widget_in_builder(self):
        """Builder page shows onboarding checklist for users without completed resumes."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('resume_update', args=[999]))  # 404 but tests template rendering
        # This will 404 but we can test the template directly
```

---

- [ ] **Step 8: Run onboarding tests**

Run: `python manage.py test accounts.tests.OnboardingTest --verbosity=1`
Expected: PASS for skip, login redirect, welcome greeting tests

---

- [ ] **Step 9: Commit**

```bash
git add accounts/onboarding_views.py accounts/urls.py templates/onboarding/welcome.html templates/resumes/onboarding_checklist.html templates/resumes/resume_builder.html resumes/views.py accounts/tests.py
git commit -m "feat(P1.1): add onboarding skip flow and checklist widget

- Add onboarding_skip and onboarding_complete views
- Welcome page shows personalized greeting with user's first name
- Skip for now link marks onboarding_completed=True
- Onboarding checklist widget in builder auto-checks filled sections
- First resume creation marks onboarding complete
- Checklist is collapsible and dismissible"
```

---

## P1.2 Dashboard

### Task 3: Dashboard Template — Stats, Activity Feed, Resume Cards

**Files:**
- Modify: `d:/EliteSttack/templates/resumes/dashboard.html`
- Create: `d:/EliteSttack/templates/resumes/widgets/stats_widget.html`
- Create: `d:/EliteSttack/templates/resumes/widgets/activity_feed.html`
- Create: `d:/EliteSttack/templates/resumes/widgets/resume_card.html`
- Modify: `d:/EliteSttack/templates/resumes/resume_list.html`

---

- [ ] **Step 1: Create stats widget template**

Create directory: `d:/EliteSttack/templates/resumes/widgets/`
Create file: `d:/EliteSttack/templates/resumes/widgets/stats_widget.html`

```html
{% load resume_extras %}

<div class="bg-white rounded-lg shadow-md p-6 mb-6">
    <div class="flex items-center justify-between">
        <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
            </svg>
            <h2 class="text-lg font-semibold text-gray-900">My Resumes</h2>
        </div>
        <a href="{% url 'resume_create' %}" class="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700">
            + Create New
        </a>
    </div>
    <div class="mt-4 grid grid-cols-3 gap-4">
        <div class="text-center">
            <div class="text-2xl font-bold text-gray-900">{{ stats.total }}</div>
            <div class="text-xs text-gray-500 uppercase tracking-wide">Total</div>
        </div>
        <div class="text-center">
            <div class="text-2xl font-bold text-green-600">{{ stats.complete }}</div>
            <div class="text-xs text-gray-500 uppercase tracking-wide">Complete</div>
        </div>
        <div class="text-center">
            <div class="text-2xl font-bold text-yellow-600">{{ stats.draft }}</div>
            <div class="text-xs text-gray-500 uppercase tracking-wide">Drafts</div>
        </div>
    </div>
    {% if stats.last_edited %}
    <div class="mt-3 text-xs text-gray-400 text-center">
        Last edited {{ stats.last_edited|relative_time }}
    </div>
    {% endif %}
</div>
```

---

- [ ] **Step 2: Create activity feed widget template**

Create file: `d:/EliteSttack/templates/resumes/widgets/activity_feed.html`

```html
{% load resume_extras %}

<div class="bg-white rounded-lg shadow-md p-4">
    <h3 class="text-sm font-semibold text-gray-700 mb-3">Recent Activity</h3>
    {% if activity %}
    <ul class="space-y-3">
        {% for resume in activity %}
        <li>
            <a href="{% url 'resume_preview' resume.id %}" class="flex items-start gap-3 group">
                <div class="mt-0.5 w-6 h-6 rounded-full bg-blue-100 flex items-center justify-center flex-shrink-0">
                    <svg class="w-3 h-3 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                    </svg>
                </div>
                <div class="flex-1 min-w-0">
                    <p class="text-sm text-gray-700 group-hover:text-blue-600 truncate">
                        Edited <span class="font-medium">{{ resume.title }}</span>
                    </p>
                    <p class="text-xs text-gray-400">{{ resume.updated_at|relative_time }}</p>
                </div>
            </a>
        </li>
        {% endfor %}
    </ul>
    {% else %}
    <p class="text-sm text-gray-400 text-center py-4">No recent activity</p>
    {% endif %}
</div>
```

---

- [ ] **Step 3: Create resume card widget template**

Create file: `d:/EliteSttack/templates/resumes/widgets/resume_card.html`

```html
{% load resume_extras %}

<div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-200 overflow-hidden">
    <!-- Card Header -->
    <div class="p-4 border-b border-gray-100">
        <div class="flex items-start justify-between">
            <div class="flex-1 min-w-0">
                <div class="flex items-center gap-2">
                    <h3 class="text-base font-semibold text-gray-900 truncate">
                        <a href="{% url 'resume_preview' resume.id %}" class="hover:text-blue-600">
                            {{ resume.title }}
                        </a>
                    </h3>
                    {% if resume.is_primary %}
                    <span class="px-2 py-0.5 text-xs font-medium bg-blue-100 text-blue-700 rounded-full">Primary</span>
                    {% endif %}
                </div>
                <p class="text-xs text-gray-400 mt-1">{{ resume.updated_at|relative_time }}</p>
            </div>
            <span class="ml-2 px-2 py-0.5 text-xs font-medium rounded-full
                {% if resume.status == 'complete' %}bg-green-100 text-green-700
                {% else %}bg-yellow-100 text-yellow-700{% endif %}">
                {{ resume.status|title }}
            </span>
        </div>
    </div>

    <!-- Card Body -->
    <div class="p-4">
        {% if resume.full_name %}
        <p class="text-sm text-gray-600 mb-1">
            <span class="font-medium text-gray-700">{{ resume.full_name }}</span>
        </p>
        {% endif %}
        <p class="text-xs text-gray-400 mb-3">
            {{ resume.education.count|default:0 }} education ·
            {{ resume.experience.count|default:0 }} experience ·
            {{ resume.skills.count|default:0 }} skills
        </p>
    </div>

    <!-- Card Actions -->
    <div class="px-4 pb-4 flex flex-wrap gap-2">
        <a href="{% url 'resume_preview' resume.id %}"
           class="px-3 py-1.5 text-xs font-medium text-blue-600 border border-blue-200 rounded-md hover:bg-blue-50">
            Preview
        </a>
        <a href="{% url 'resume_update' resume.id %}"
           class="px-3 py-1.5 text-xs font-medium text-gray-600 border border-gray-200 rounded-md hover:bg-gray-50">
            Edit
        </a>
        <a href="{% url 'resume_export_pdf' resume.id %}" target="_blank"
           class="px-3 py-1.5 text-xs font-medium text-gray-600 border border-gray-200 rounded-md hover:bg-gray-50 flex items-center gap-1">
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
            </svg>
            PDF
        </a>
        <form method="POST" action="{% url 'set_primary' resume.id %}" class="inline">
            {% csrf_token %}
            <button type="submit"
                    class="px-3 py-1.5 text-xs font-medium text-gray-600 border border-gray-200 rounded-md hover:bg-gray-50
                           {% if resume.is_primary %}border-blue-200 bg-blue-50 text-blue-600{% endif %}">
                Set Primary
            </button>
        </form>
        <a href="{% url 'resume_duplicate' resume.id %}"
           class="px-3 py-1.5 text-xs font-medium text-gray-600 border border-gray-200 rounded-md hover:bg-gray-50">
            Duplicate
        </a>
        <a href="{% url 'resume_delete' resume.id %}"
           class="px-3 py-1.5 text-xs font-medium text-red-600 border border-red-200 rounded-md hover:bg-red-50">
            Delete
        </a>
    </div>
</div>
```

---

- [ ] **Step 4: Rewrite the dashboard template**

Read `d:/EliteSttack/templates/resumes/dashboard.html`. Replace all content with:

```html
{% extends 'base.html' %}
{% load resume_extras %}
{% block title %}Dashboard - Placement Copilot{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto mt-8 px-4 pb-12">
    <!-- Stats Widget -->
    {% include 'resumes/widgets/stats_widget.html' %}

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Resume Cards (2/3 width) -->
        <div class="lg:col-span-2">
            <h2 class="text-xl font-bold text-gray-900 mb-4">Your Resumes</h2>

            {% if resumes %}
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {% for resume in resumes %}
                    {% include 'resumes/widgets/resume_card.html' %}
                {% endfor %}
            </div>
            {% else %}
            <!-- Empty State -->
            <div class="bg-white rounded-lg shadow-md p-12 text-center">
                <div class="w-16 h-16 mx-auto bg-blue-100 rounded-full flex items-center justify-center mb-4">
                    <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                </div>
                <h3 class="text-lg font-semibold text-gray-900 mb-2">No resumes yet</h3>
                <p class="text-gray-500 mb-6">Create your first resume and take the next step in your career.</p>
                <a href="{% url 'resume_create' %}" class="inline-flex items-center px-6 py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700">
                    Create your first resume
                </a>
            </div>
            {% endif %}
        </div>

        <!-- Activity Feed (1/3 width) -->
        <div class="lg:col-span-1">
            {% include 'resumes/widgets/activity_feed.html' %}
        </div>
    </div>
</div>
{% endblock %}
```

---

- [ ] **Step 5: Update resume_list template to use the same card**

Read `d:/EliteSttack/templates/resumes/resume_list.html`. Replace the resume cards section with:

```html
        {% if resumes %}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {% for resume in resumes %}
                {% include 'resumes/widgets/resume_card.html' %}
            {% endfor %}
        </div>
        {% else %}
        <!-- Empty State -->
        <div class="bg-white rounded-lg shadow-md p-12 text-center">
            <div class="w-16 h-16 mx-auto bg-blue-100 rounded-full flex items-center justify-center mb-4">
                <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">No resumes yet</h3>
            <p class="text-gray-500 mb-6">Create your first resume and take the next step in your career.</p>
            <a href="{% url 'resume_create' %}" class="inline-flex items-center px-6 py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700">
                Create your first resume
            </a>
        </div>
        {% endif %}
```

Remove the "My Resumes" heading and top buttons if present, since the dashboard now handles that.

---

- [ ] **Step 6: Write dashboard tests**

Add to `d:/EliteSttack/resumes/tests.py`:

```python
from django.test import TestCase, Client
from django.contrib.auth.models import User
from django.urls import reverse
from resumes.models import Resume


class DashboardTest(TestCase):
    """Tests for dashboard stats and activity feed."""

    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )

    def test_dashboard_stats_shows_correct_counts(self):
        """Dashboard displays correct total, complete, and draft counts."""
        self.client.login(username='testuser', password='testpass123')
        # Create resumes
        r1 = Resume.objects.create(user=self.user, title='Resume 1', status='draft')
        r2 = Resume.objects.create(user=self.user, title='Resume 2', status='complete')
        r3 = Resume.objects.create(user=self.user, title='Resume 3', status='draft')

        response = self.client.get(reverse('dashboard'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, '3')  # total
        self.assertContains(response, '2')  # drafts
        self.assertContains(response, '1')  # complete

    def test_dashboard_shows_activity_feed(self):
        """Dashboard shows recent activity for last 5 resumes."""
        self.client.login(username='testuser', password='testpass123')
        for i in range(6):
            Resume.objects.create(user=self.user, title=f'Resume {i}')
        response = self.client.get(reverse('dashboard'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Recent Activity')
        # Only 5 items in activity feed
        self.assertContains(response, 'Resume 5')  # most recent

    def test_dashboard_requires_authentication(self):
        """Unauthenticated users are redirected to login."""
        response = self.client.get(reverse('dashboard'))
        self.assertEqual(response.status_code, 302)
        self.assertIn('login', response.url)
```

---

- [ ] **Step 7: Run dashboard tests**

Run: `python manage.py test resumes.tests.DashboardTest --verbosity=1`
Expected: 3 PASS

---

- [ ] **Step 8: Commit**

```bash
git add templates/resumes/dashboard.html templates/resumes/resume_list.html templates/resumes/widgets/
git commit -m "feat(P1.2): rebuild dashboard with stats widget, activity feed, and resume cards

- Stats widget: total/complete/draft counts + last edited
- Activity feed: last 5 edited resumes with relative timestamps
- Resume cards: status badge, primary badge, action buttons
- Empty state with illustration and CTA
- Responsive grid: 2-col cards + activity sidebar on desktop"
```

---

## P1.3 Templates

### Task 4: Template Gallery Page

**Files:**
- Modify: `d:/EliteSttack/resumes/urls.py`
- Create: `d:/EliteSttack/resumes/template_views.py`
- Create: `d:/EliteSttack/templates/resumes/template_gallery.html`

---

- [ ] **Step 1: Create template gallery view**

Create file: `d:/EliteSttack/resumes/template_views.py`

```python
from django.shortcuts import render
from django.contrib.auth.decorators import login_required


@login_required
def template_gallery(request):
    """Show all available resume templates with preview."""
    templates = [
        {
            'id': 'classic',
            'name': 'Classic',
            'description': 'Traditional, serif fonts, standard margins — conservative and widely accepted across industries.',
            'best_for': 'Conservative industries: law, finance, government',
            'ats_friendly': True,
        },
        {
            'id': 'modern',
            'name': 'Modern',
            'description': 'Sans-serif, strong visual hierarchy, blue accents — clean and contemporary.',
            'best_for': 'Tech, startup, creative roles',
            'ats_friendly': True,
        },
        {
            'id': 'minimal',
            'name': 'Minimal',
            'description': 'Maximum whitespace, simple typography — elegant and highly ATS-friendly.',
            'best_for': 'All industries, ATS-optimized applications',
            'ats_friendly': True,
        },
    ]
    return render(request, 'resumes/template_gallery.html', {'templates': templates})
```

---

- [ ] **Step 2: Add template gallery URL**

Read `d:/EliteSttack/resumes/urls.py`. Add to urlpatterns:

```python
    path('templates/', views.template_gallery, name='template_gallery'),
```

Also update the import at the top:
```python
from . import views, template_views
```

And update the router to include template_views in imports.

---

- [ ] **Step 3: Create template gallery template**

Create file: `d:/EliteSttack/templates/resumes/template_gallery.html`

```html
{% extends 'base.html' %}
{% block title %}Resume Templates - Placement Copilot{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto py-8 px-4">
    <!-- Header -->
    <div class="text-center mb-10">
        <h1 class="text-3xl font-bold text-gray-900 mb-3">Resume Templates</h1>
        <p class="text-gray-600 max-w-2xl mx-auto">
            Choose from our professionally designed templates. All templates are ATS-friendly and work with any industry.
        </p>
    </div>

    <!-- Template Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-10">
        {% for template in templates %}
        <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300">
            <!-- Preview Area -->
            <div class="h-80 bg-gray-100 overflow-hidden relative">
                <iframe
                    src="{% url 'template_preview' template.id %}"
                    class="w-full h-full border-0"
                    title="{{ template.name }} template preview"
                    loading="lazy"
                ></iframe>
            </div>

            <!-- Template Info -->
            <div class="p-6">
                <div class="flex items-start justify-between mb-2">
                    <h2 class="text-xl font-bold text-gray-900">{{ template.name }}</h2>
                    {% if template.ats_friendly %}
                    <span class="px-2 py-0.5 text-xs font-medium bg-green-100 text-green-700 rounded-full">ATS ✓</span>
                    {% endif %}
                </div>
                <p class="text-gray-600 text-sm mb-3">{{ template.description }}</p>
                <p class="text-xs text-gray-400 mb-4">
                    <span class="font-medium">Best for:</span> {{ template.best_for }}
                </p>
                <a href="{% url 'resume_create' %}?template={{ template.id }}"
                   class="block w-full text-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700">
                    Use This Template
                </a>
            </div>
        </div>
        {% endfor %}
    </div>

    <!-- Info Banner -->
    <div class="bg-blue-50 rounded-xl p-6 text-center">
        <p class="text-sm text-blue-800">
            All templates support both US Letter and A4 paper sizes. You can switch templates anytime in the builder without losing your content.
        </p>
    </div>
</div>
{% endblock %}
```

---

- [ ] **Step 4: Add template preview iframe view**

Add to `d:/EliteSttack/resumes/template_views.py`:

```python
from django.shortcuts import render, get_object_or_404
from resumes.models import Resume


@login_required
def template_preview(request, template_id):
    """Render a sample resume in the specified template for gallery preview."""
    valid_templates = ['classic', 'modern', 'minimal']
    if template_id not in valid_templates:
        template_id = 'classic'

    # Create a minimal sample resume for preview
    sample_resume_data = {
        'title': 'Sarah Chen',
        'full_name': 'Sarah Chen',
        'email': 'sarah.chen@email.com',
        'phone': '(555) 123-4567',
        'location': 'San Francisco, CA',
        'linkedin': 'linkedin.com/in/sarahchen',
        'professional_summary': 'Experienced software engineer with 5+ years building scalable web applications. Passionate about clean code and user experience.',
        'template_style': template_id,
        'education': [],
        'experience': [],
        'projects': [],
        'skills': [],
    }

    class SampleResume:
        def __init__(self, data):
            for k, v in data.items():
                setattr(self, k, v)
        def __iter__(self):
            return iter([])
        def exists(self):
            return False
        def all(self):
            return []
        def count(self):
            return 0
        def filter(self, *a, **k):
            return type('empty', (), {'exists': lambda: False, 'all': lambda: [], 'count': lambda: 0})()

    sample = SampleResume(sample_resume_data)
    template_name = f'resumes/resume_{template_id}.html'
    return render(request, template_name, {'resume': sample})
```

---

- [ ] **Step 5: Add template preview URL**

Add to `d:/EliteSttack/resumes/urls.py`:

```python
    path('templates/<str:template_id>/preview/', views.template_preview, name='template_preview'),
```

Also add to the import:
```python
from . import views, template_views
```

---

- [ ] **Step 6: Update resume_create to respect default_template**

Read `d:/EliteSttack/resumes/views.py` lines 44-71. In `resume_create`, after `form = ResumeForm(request.POST)`:

```python
            # Use user's default template or query param
            template_style = request.GET.get('template', None)
            if not template_style:
                template_style = getattr(request.user.profile, 'default_template', 'classic')
            resume.template_style = template_style
```

And after `resume.save()`:
```python
            # Use template_style from POST or default
            template_style = request.POST.get('template_style', None)
            if not template_style:
                template_style = getattr(request.user.profile, 'default_template', 'classic')
            resume.template_style = template_style
            resume.save(update_fields=['template_style'])
```

Wait, these duplicate the existing form save. Instead, modify the form save to also set template_style. Read the ResumeForm definition in `d:/EliteSttack/resumes/forms.py` first. The ResumeForm likely handles `template_style`. If it does, the simplest approach is to pass `initial={'template_style': default}` in the GET handler.

In the GET branch of `resume_create`:
```python
    else:
        form = ResumeForm(initial={'template_style': getattr(request.user.profile, 'default_template', 'classic')})
```

And in the POST branch, after `resume = form.save(commit=False)`:
```python
            resume.template_style = request.POST.get('template_style', getattr(request.user.profile, 'default_template', 'classic'))
```

---

- [ ] **Step 7: Add template gallery link to nav**

Read `d:/EliteSttack/templates/base.html`. Add a "Templates" link to the navigation bar next to "My Resumes":

```html
<a href="{% url 'template_gallery' %}"
   class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium
          {% if request.resolver_match.url_name == 'template_gallery' %}text-blue-600 border-b-2 border-blue-600{% endif %}">
    Templates
</a>
```

---

- [ ] **Step 8: Write template gallery tests**

Add to `d:/EliteSttack/resumes/tests.py`:

```python
from django.test import TestCase, Client
from django.contrib.auth.models import User
from django.urls import reverse


class TemplateGalleryTest(TestCase):
    """Tests for template gallery."""

    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )

    def test_gallery_shows_all_three_templates(self):
        """Template gallery displays Classic, Modern, and Minimal."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('template_gallery'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Classic')
        self.assertContains(response, 'Modern')
        self.assertContains(response, 'Minimal')
        self.assertContains(response, 'ATS')

    def test_gallery_requires_authentication(self):
        """Unauthenticated users are redirected to login."""
        response = self.client.get(reverse('template_gallery'))
        self.assertEqual(response.status_code, 302)
        self.assertIn('login', response.url)

    def test_template_preview_renders(self):
        """Template preview iframe renders without error."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('template_preview', args=['classic']))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Sarah Chen')

    def test_template_preview_invalid_falls_back_to_classic(self):
        """Invalid template ID falls back to classic."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('template_preview', args=['invalid']))
        self.assertEqual(response.status_code, 200)
```

---

- [ ] **Step 9: Run template gallery tests**

Run: `python manage.py test resumes.tests.TemplateGalleryTest --verbosity=1`
Expected: 4 PASS

---

- [ ] **Step 10: Commit**

```bash
git add resumes/template_views.py resumes/urls.py resumes/views.py templates/resumes/template_gallery.html templates/base.html resumes/tests.py
git commit -m "feat(P1.3): add template gallery page with live previews

- Template gallery at /templates/ shows all 3 templates
- Each template renders a sample resume in an iframe
- Template selection pre-fills resume creation form
- User's default template used for new resumes
- Add Templates link to navigation bar"
```

---

## P1.4 Settings

### Task 5: Settings — Preferences Tab (Default Template, Paper Size, Email)

**Files:**
- Modify: `d:/EliteSttack/accounts/settings_views.py`
- Modify: `d:/EliteSttack/accounts/forms.py`
- Modify: `d:/EliteSttack/accounts/urls.py`
- Modify: `d:/EliteSttack/templates/settings/settings.html`

---

- [ ] **Step 1: Add ProfilePreferencesForm to accounts/forms.py**

Read `d:/EliteSttack/accounts/forms.py`. Add after the `CustomPasswordChangeForm` class:

```python
from .models import UserProfile


class ProfilePreferencesForm(forms.ModelForm):
    """Form for updating user preferences."""

    class Meta:
        model = UserProfile
        fields = ('default_template', 'default_paper_size', 'email_opt_out')

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.fields['default_template'].widget.attrs.update({
            'class': 'w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'
        })
        self.fields['default_paper_size'].widget.attrs.update({
            'class': 'w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500'
        })
        self.fields['email_opt_out'].widget.attrs.update({
            'class': 'rounded border-gray-300 text-blue-600 focus:ring-blue-500'
        })
```

---

- [ ] **Step 2: Add preferences view to accounts/settings_views.py**

Read `d:/EliteSttack/accounts/settings_views.py`. Add after the `settings_delete_account` function:

```python
from .forms import ProfileForm, CustomPasswordChangeForm, ProfilePreferencesForm
from .models import UserProfile


@login_required
def settings_preferences(request):
    """Update user preferences (template, paper size, email)."""
    if request.method == 'POST':
        form = ProfilePreferencesForm(request.POST, instance=request.user.profile)
        if form.is_valid():
            form.save()
            messages.success(request, 'Preferences saved successfully.')
            return redirect('settings')
        else:
            messages.error(request, 'Please correct the errors below.')
    else:
        form = ProfilePreferencesForm(instance=request.user.profile)

    return redirect('settings')
```

Wait, this redirects — for AJAX or form submission, the preferences form should be handled in the main settings view. Update the main `settings` view instead:

Replace `d:/EliteSttack/accounts/settings_views.py` entirely with:

```python
from django.shortcuts import render, redirect
from django.contrib.auth import logout
from django.contrib.auth.decorators import login_required
from django.contrib import messages
from django.contrib.auth import update_session_auth_hash
from .forms import ProfileForm, CustomPasswordChangeForm, ProfilePreferencesForm


@login_required
def settings(request):
    """Settings page - shows all forms."""
    profile_form = ProfileForm(instance=request.user)
    password_form = CustomPasswordChangeForm(request.user)
    preferences_form = ProfilePreferencesForm(instance=request.user.profile)

    if request.method == 'POST':
        if 'profile_submit' in request.POST:
            profile_form = ProfileForm(request.POST, instance=request.user)
            if profile_form.is_valid():
                profile_form.save()
                messages.success(request, 'Profile updated successfully.')
        elif 'password_submit' in request.POST:
            password_form = CustomPasswordChangeForm(request.user, request.POST)
            if password_form.is_valid():
                user = password_form.save()
                update_session_auth_hash(request, user)
                messages.success(request, 'Password updated successfully.')
            else:
                messages.error(request, 'Please correct the errors below.')
        elif 'preferences_submit' in request.POST:
            preferences_form = ProfilePreferencesForm(request.POST, instance=request.user.profile)
            if preferences_form.is_valid():
                preferences_form.save()
                messages.success(request, 'Preferences saved successfully.')
            else:
                messages.error(request, 'Please correct the errors below.')
        return redirect('settings')

    return render(request, 'settings/settings.html', {
        'profile_form': profile_form,
        'password_form': password_form,
        'preferences_form': preferences_form,
    })


@login_required
def settings_profile(request):
    """Update profile information (legacy redirect)."""
    return redirect('settings')


@login_required
def settings_password(request):
    """Change password (legacy redirect)."""
    return redirect('settings')


@login_required
def settings_delete_account(request):
    """Delete user account."""
    if request.method == 'POST':
        username = request.user.username
        request.user.delete()
        logout(request)
        messages.success(request, f'Account for {username} has been deleted.')
        return redirect('login')

    return redirect('settings')
```

---

- [ ] **Step 3: Rewrite settings template with tabs**

Read `d:/EliteSttack/templates/settings/settings.html`. Replace all content with:

```html
{% extends 'base.html' %}
{% block title %}Settings - Placement Copilot{% endblock %}

{% block content %}
<div class="max-w-3xl mx-auto py-8 px-4">
    <h1 class="text-3xl font-bold text-gray-900 mb-8">Settings</h1>

    <!-- Messages -->
    {% if messages %}
    <div class="mb-6">
        {% for message in messages %}
        <div class="p-4 rounded-lg {% if message.tags == 'success' %}bg-green-100 border border-green-400 text-green-700{% elif message.tags == 'error' %}bg-red-100 border border-red-400 text-red-700{% else %}bg-blue-100 border border-blue-400 text-blue-700{% endif %}">
            {{ message }}
        </div>
        {% endfor %}
    </div>
    {% endif %}

    <!-- Tab Navigation -->
    <div class="border-b border-gray-200 mb-6">
        <nav class="-mb-px flex space-x-6" id="settings-tabs">
            <button data-tab="profile"
                    class="settings-tab pb-3 px-1 border-b-2 text-sm font-medium text-blue-600 border-blue-600">
                Profile
            </button>
            <button data-tab="preferences"
                    class="settings-tab pb-3 px-1 border-b-2 text-sm font-medium text-gray-500 border-transparent hover:text-gray-700 hover:border-gray-300">
                Preferences
            </button>
            <button data-tab="security"
                    class="settings-tab pb-3 px-1 border-b-2 text-sm font-medium text-gray-500 border-transparent hover:text-gray-700 hover:border-gray-300">
                Security
            </button>
            <button data-tab="account"
                    class="settings-tab pb-3 px-1 border-b-2 text-sm font-medium text-gray-500 border-transparent hover:text-gray-700 hover:border-gray-300">
                Account
            </button>
        </nav>
    </div>

    <!-- Profile Tab -->
    <div id="tab-profile" class="settings-panel">
        <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Profile Information</h2>
            <form method="POST" action="{% url 'settings' %}">
                {% csrf_token %}
                <input type="hidden" name="profile_submit" value="1">
                <div class="space-y-4">
                    <div>
                        <label for="id_username" class="block text-sm font-medium text-gray-700 mb-1">Username</label>
                        <input type="text" name="username" id="id_username"
                               value="{{ request.user.username }}"
                               class="w-full px-4 py-2 border rounded-lg bg-gray-50" readonly>
                    </div>
                    <div>
                        <label for="id_email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                        <input type="email" name="email" id="id_email"
                               value="{{ request.user.email }}"
                               class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                </div>
                <button type="submit" class="mt-4 px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
                    Save Profile
                </button>
            </form>
        </div>
    </div>

    <!-- Preferences Tab -->
    <div id="tab-preferences" class="settings-panel hidden">
        <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Resume Preferences</h2>
            <form method="POST" action="{% url 'settings' %}">
                {% csrf_token %}
                <input type="hidden" name="preferences_submit" value="1">
                <div class="space-y-6">

                    <!-- Default Template -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-3">Default Resume Template</label>
                        <div class="grid grid-cols-3 gap-3">
                            {% for value, label in preferences_form.fields.default_template.choices %}
                            <label class="cursor-pointer">
                                <input type="radio" name="default_template" value="{{ value }}"
                                       class="sr-only peer"
                                       {% if preferences_form.instance.default_template == value %}checked{% endif %}>
                                <div class="border-2 border-gray-200 rounded-lg p-3 text-center hover:border-blue-300 peer-checked:border-blue-600 peer-checked:bg-blue-50 transition-colors">
                                    <span class="text-sm font-medium text-gray-700">{{ label }}</span>
                                </div>
                            </label>
                            {% endfor %}
                        </div>
                    </div>

                    <!-- Paper Size -->
                    <div>
                        <label for="id_default_paper_size" class="block text-sm font-medium text-gray-700 mb-1">Default Paper Size</label>
                        <select name="default_paper_size" id="id_default_paper_size"
                                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                            {% for value, label in preferences_form.fields.default_paper_size.choices %}
                            <option value="{{ value }}" {% if preferences_form.instance.default_paper_size == value %}selected{% endif %}>
                                {{ label }}
                            </option>
                            {% endfor %}
                        </select>
                    </div>

                    <!-- Email Opt-Out -->
                    <div class="flex items-start gap-3">
                        <input type="checkbox" name="email_opt_out" id="id_email_opt_out"
                               class="mt-1 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
                               {% if preferences_form.instance.email_opt_out %}checked{% endif %}>
                        <div>
                            <label for="id_email_opt_out" class="text-sm font-medium text-gray-700">
                                Opt out of marketing emails
                            </label>
                            <p class="text-xs text-gray-500 mt-0.5">
                                You'll still receive transactional emails about your account and resume exports.
                            </p>
                        </div>
                    </div>
                </div>
                <button type="submit" class="mt-6 px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
                    Save Preferences
                </button>
            </form>
        </div>
    </div>

    <!-- Security Tab -->
    <div id="tab-security" class="settings-panel hidden">
        <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Change Password</h2>
            <form method="POST" action="{% url 'settings' %}">
                {% csrf_token %}
                <input type="hidden" name="password_submit" value="1">
                <div class="space-y-4">
                    <div>
                        <label for="id_old_password" class="block text-sm font-medium text-gray-700 mb-1">Current Password</label>
                        <input type="password" name="old_password" id="id_old_password"
                               class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div>
                        <label for="id_new_password1" class="block text-sm font-medium text-gray-700 mb-1">New Password</label>
                        <input type="password" name="new_password1" id="id_new_password1"
                               class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div>
                        <label for="id_new_password2" class="block text-sm font-medium text-gray-700 mb-1">Confirm New Password</label>
                        <input type="password" name="new_password2" id="id_new_password2"
                               class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                </div>
                <button type="submit" class="mt-4 px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
                    Update Password
                </button>
            </form>
        </div>
    </div>

    <!-- Account Tab -->
    <div id="tab-account" class="settings-panel hidden">
        <div class="bg-white rounded-lg shadow-md p-6 mb-6">
            <h2 class="text-xl font-semibold text-gray-900 mb-4">Account Information</h2>
            <dl class="space-y-2 text-sm">
                <div class="flex gap-4">
                    <dt class="text-gray-500 w-32">Username</dt>
                    <dd class="text-gray-900 font-medium">{{ request.user.username }}</dd>
                </div>
                <div class="flex gap-4">
                    <dt class="text-gray-500 w-32">Email</dt>
                    <dd class="text-gray-900 font-medium">{{ request.user.email }}</dd>
                </div>
                <div class="flex gap-4">
                    <dt class="text-gray-500 w-32">Member since</dt>
                    <dd class="text-gray-900 font-medium">{{ request.user.date_joined|date:"F j, Y" }}</dd>
                </div>
                <div class="flex gap-4">
                    <dt class="text-gray-500 w-32">Resumes</dt>
                    <dd class="text-gray-900 font-medium">{{ request.user.resumes.count }}</dd>
                </div>
            </dl>
        </div>

        <!-- Danger Zone -->
        <div class="bg-white rounded-lg shadow-md p-6 border-2 border-red-200">
            <h2 class="text-xl font-semibold text-red-600 mb-4">Danger Zone</h2>
            <p class="text-gray-600 mb-4">
                Once you delete your account, there is no going back. All your data will be permanently removed.
            </p>
            <form method="POST" action="{% url 'settings_delete_account' %}"
                  onsubmit="return confirm('Are you sure you want to delete your account? This cannot be undone.');">
                {% csrf_token %}
                <button type="submit" class="px-6 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700">
                    Delete Account
                </button>
            </form>
        </div>
    </div>
</div>

<script>
(function() {
    var tabs = document.querySelectorAll('.settings-tab');
    var panels = document.querySelectorAll('.settings-panel');

    tabs.forEach(function(tab) {
        tab.addEventListener('click', function() {
            var target = tab.dataset.tab;

            // Update tab styles
            tabs.forEach(function(t) {
                t.classList.remove('text-blue-600', 'border-blue-600');
                t.classList.add('text-gray-500', 'border-transparent');
            });
            tab.classList.remove('text-gray-500', 'border-transparent');
            tab.classList.add('text-blue-600', 'border-blue-600');

            // Show target panel, hide others
            panels.forEach(function(panel) {
                panel.classList.add('hidden');
            });
            var targetPanel = document.getElementById('tab-' + target);
            if (targetPanel) targetPanel.classList.remove('hidden');
        });
    });

    // Auto-select tab from URL hash
    var hash = window.location.hash.replace('#', '');
    if (hash) {
        var targetTab = document.querySelector('[data-tab="' + hash + '"]');
        if (targetTab) targetTab.click();
    }
})();
</script>
{% endblock %}
```

---

- [ ] **Step 4: Update urls.py to remove legacy settings sub-URLs**

Read `d:/EliteSttack/accounts/urls.py`. The `settings` view now handles all sections, so the sub-URLs can be simplified. Update to:

```python
    path('settings/', settings_views.settings, name='settings'),
    path('settings/delete/', settings_views.settings_delete_account, name='settings_delete_account'),
```

Remove:
```python
    path('settings/profile/', settings_views.settings_profile, name='settings_profile'),
    path('settings/password/', settings_views.settings_password, name='settings_password'),
```

---

- [ ] **Step 5: Write settings tests**

Add to `d:/EliteSttack/accounts/tests.py`:

```python
from django.test import TestCase, Client
from django.contrib.auth.models import User
from django.urls import reverse


class SettingsTest(TestCase):
    """Tests for settings page and preferences."""

    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123'
        )

    def test_settings_page_loads(self):
        """Settings page loads for authenticated user."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.get(reverse('settings'))
        self.assertEqual(response.status_code, 200)
        self.assertContains(response, 'Profile')
        self.assertContains(response, 'Preferences')

    def test_preferences_saved_correctly(self):
        """Saving preferences updates UserProfile."""
        self.client.login(username='testuser', password='testpass123')
        response = self.client.post(reverse('settings'), {
            'preferences_submit': '1',
            'default_template': 'modern',
            'default_paper_size': 'a4',
            'email_opt_out': 'on',
        })
        self.user.profile.refresh_from_db()
        self.assertEqual(self.user.profile.default_template, 'modern')
        self.assertEqual(self.user.profile.default_paper_size, 'a4')
        self.assertTrue(self.user.profile.email_opt_out)

    def test_template_default_applied_to_new_resume(self):
        """New resume uses user's default template."""
        self.client.login(username='testuser', password='testpass123')
        self.user.profile.default_template = 'modern'
        self.user.profile.save()
        response = self.client.post(reverse('resume_create'), {
            'title': 'Test Resume',
            'full_name': 'Test User',
            'email': 'test@example.com',
            'template_style': 'modern',
        }, follow=True)
        # Check a resume was created with the right template
        from resumes.models import Resume
        resume = Resume.objects.filter(user=self.user).first()
        self.assertIsNotNone(resume)
        self.assertEqual(resume.template_style, 'modern')

    def test_settings_requires_authentication(self):
        """Unauthenticated users are redirected to login."""
        response = self.client.get(reverse('settings'))
        self.assertEqual(response.status_code, 302)
        self.assertIn('login', response.url)
```

---

- [ ] **Step 6: Run settings tests**

Run: `python manage.py test accounts.tests.SettingsTest --verbosity=1`
Expected: PASS for all

---

- [ ] **Step 7: Commit**

```bash
git add accounts/settings_views.py accounts/forms.py accounts/urls.py templates/settings/settings.html accounts/tests.py
git commit -m "feat(P1.4): add preferences tab to settings page

- Tabbed settings: Profile, Preferences, Security, Account
- Preferences: default template, paper size, email opt-out
- Account info: username, email, join date, resume count
- Danger zone with account deletion
- ProfilePreferencesForm wired to UserProfile model"
```

---

## Verification

After all tasks complete:

1. Run: `python manage.py check`
   Expected: 0 issues

2. Run: `python manage.py test accounts.tests resumes.tests --verbosity=1`
   Expected: All tests pass

3. Manually verify:
   - Register a new user → redirected to welcome page with personalized greeting
   - Click "Skip for now" → redirected to dashboard, no resumes shown
   - Create a resume → onboarding checklist appears in builder
   - Dashboard shows stats, resume cards, and activity feed
   - Visit `/templates/` → all 3 template previews render
   - Visit `/settings/` → profile, preferences, security, account tabs work
   - Change default template in preferences → new resumes use it

---

## Spec Coverage

| Spec Requirement | Task |
|---|---|
| UserProfile with onboarding_completed | Task 1 |
| Welcome page with first name | Task 2 |
| Skip for now → marks onboarding complete | Task 2 |
| Onboarding checklist in builder | Task 2 |
| First resume → marks onboarding complete | Task 2 |
| Dashboard stats widget | Task 3 |
| Dashboard activity feed | Task 3 |
| Dashboard resume cards | Task 3 |
| Dashboard empty state | Task 3 |
| Template gallery at /templates/ | Task 4 |
| Default template from UserProfile | Tasks 1, 4 |
| Settings preferences tab | Task 5 |
| Paper size preference | Task 5 |
| Email opt-out | Task 5 |
| Account info in settings | Task 5 |
| Account deletion | Task 5 (already wired) |
